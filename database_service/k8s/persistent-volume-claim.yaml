apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: postgres-pvc # Name of the PersistentVolumeClaim

spec:
  accessModes:
    - ReadWriteOnce # Storage can be mounted by one node for read/write

  resources:
    requests:
      storage: 1Gi # Requests 1 GB of persistent storage