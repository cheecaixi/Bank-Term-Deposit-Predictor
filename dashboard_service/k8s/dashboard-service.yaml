apiVersion: v1
kind: Service

metadata:
  name: dashboard-service

spec:
  type: NodePort

  selector:
    app: dashboard-service

  ports:
    - protocol: TCP
      port: 8501
      targetPort: 8501
      nodePort: 30085