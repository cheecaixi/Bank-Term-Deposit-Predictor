# Dashboard Configuration
# Stores non-sensitive configuration values used by the Streamlit Dashboard

apiVersion: v1
kind: ConfigMap
metadata:
  name: dashboard-config
data:
  # Internal Kubernetes Service URL of the API Gateway
  # The Dashboard uses this URL to communicate with the API Gateway
  GATEWAY_URL: "http://api-gateway:8080"